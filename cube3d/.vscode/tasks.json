{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C Program",
            "type": "shell",
            "command": "make",  // Command to build the program
            "args": [],  // Arguments for the build command
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],  // Problem matcher for GCC
            "detail": "Generated task for building the C program using make"
        }
    ]
}